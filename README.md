# Vertical-Timeline
A Vertical Timeline is a UI component used to visually display events in chronological order. It is commonly used for project milestones, educational history, work experience, or product roadmaps. This version is fully responsive and styled using pure HTML and CSS.
# Vertical Timeline

A clean and responsive Vertical Timeline built using HTML and CSS.  
This component visually represents events in chronological order with an alternating left-right layout.

## 📌 Features

- Vertical center line design
- Alternating left-right event layout
- Responsive for mobile devices
- Pure HTML & CSS (No JavaScript required)
- Easy to customize and extend

## 🛠️ Technologies Used

- HTML5
- CSS3 (Flexbox, Positioning, Media Queries)

---

## 📂 Project Structure

Vertical-Timeline/
│
├── v.html
├── vv.css
└── README.md

## 🚀 How to Run

1. Download or clone the repository.
2. Open the project folder.
3. Open `index.html` in any modern web browser.

No additional setup required.

## 📖 How It Works

- The vertical center line is created using the `::after` pseudo-element on the `.timeline` container.
- Each timeline event is placed inside a `.container`.
- Left and right alignment is controlled using CSS positioning.
- Circles are created using `::after` pseudo-elements.
- Media queries make the layout responsive on smaller screens.

## 🎨 Customization

- Change colors inside `style.css`.
- Add more timeline events in `index.html`.
- Modify spacing and fonts to match your design.
- Convert to dark theme by changing background and text colors.

## 📌 Use Cases

- Educational history
- Work experience
- Project roadmap
- Product launch timeline
- Personal portfolio section
